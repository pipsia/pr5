# Угадай число (межпроцессное взаимодействие)

Программа создает два процесса, которые угадывают числа друг у друга с помощью сигналов.

## Компиляция и запуск
```bash
gcc guess_number.c -o guess_number
./guess_number 100
